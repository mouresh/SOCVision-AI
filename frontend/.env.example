# SOCVision AI — Frontend Environment Configuration Template
# Copy to .env and fill in values. Never commit .env to version control.

# Active backend API URL (Render production URL)
VITE_API_URL=https://socvision-api.onrender.com/api/v1
